'use strict';

describe('myApp.double-random module', function() {

  beforeEach(module('myApp.double-random'));

  describe('double-random controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var doubleRandomCtrl = $controller('doubleRandomCtrl');
      expect(doubleRandomCtrl).toBeDefined();
    }));

  });
});
